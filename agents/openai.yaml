interface:
  display_name: "Dataset Research"
  short_description: "Find, screen, and map research datasets"
  default_prompt: "Use $dataset-research to find public datasets for a topic, audit access, extract core dimensions, deduplicate records, and propose visualizations."
